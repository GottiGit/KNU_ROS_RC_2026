[project]
name = "rc-car-ui"
version = "0.1.0"
description = "RC car control UI - Flask backend + pywebview frontend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pywebview[gtk]>=6.2.1",
]